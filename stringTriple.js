const nombres=[
"suv",
"lmn",
"giants",
"cher2",
"kmo",
"hosefv"
];

function selecionarStrings(){ 

for (var l of nombres){
    if(l.length ===3){
        console.log(l);
    }
};
};

selecionarStrings();

